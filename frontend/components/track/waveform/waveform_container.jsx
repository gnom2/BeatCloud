// import WaveForm from "./waveform";
// import { connect } from "react-redux";
// import { updatePlaypoint } from "../../../actions/track_player_actions";

// const msp = (state, ownProps) => ({
//   track: ownProps.track,
//   audioPlayer: ownProps.audioPlayer,
//   trackPlayer: state.ui.trackPlayer,
//   barHeight: ownProps.barHeight,
// });

// const mdp = (dispatch) => ({
//   updatePlaypoint: (newTime) => dispatch(updatePlaypoint(newTime)),
// });

// export default connect(msp, mdp)(WaveForm);
